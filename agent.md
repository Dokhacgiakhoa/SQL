# SQL Syllabus Mind Map (Generated from current Question Bank)

> [!NOTE]
> This mind map is generated based on the structure of `questions.py` as I cannot directly read the text from `SEM 1.png`. Please use this to verify if the codebase matches your intended syllabus.

```mermaid
mindmap
  root((SQL SEM 1))
    Introduction
      Definitions (SQL, RDBMS)
      Basic Commands
        SELECT
        INSERT
        UPDATE
        DELETE
    DDL (Data Definition)
      CREATE TABLE
      DROP TABLE
      ALTER TABLE
      TRUNCATE
      Data Types (INT, VARCHAR, DATE)
    Constraints
      PRIMARY KEY
      FOREIGN KEY
      NOT NULL
      UNIQUE
      CHECK / DEFAULT
    Querying Data
      Filtering (WHERE)
      Sorting (ORDER BY)
      Logic (AND, OR, NOT)
      Operators (LIKE, IN, BETWEEN, IS NULL)
    Functions
      Aggregation (COUNT, SUM, AVG, MIN, MAX)
      String (CONCAT, LEN, UPPER)
      Date (GETDATE)
    Joins & Combination
      INNER JOIN
      LEFT / RIGHT JOIN
      FULL OUTER JOIN
      UNION / UNION ALL
    Grouping
      GROUP BY
      HAVING
    Advanced Topics (Check Scope)
      Normalization
        1NF
        2NF
        3NF
      T-SQL Objects
        Stored Procedures
        Views
        Triggers
      Transactions
        ACID Properties
        COMMIT / ROLLBACK
        Deadlock
      Performance & Security
        Indexes
        SQL Injection
        GRANT / REVOKE
```
